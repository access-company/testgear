/_build
/_build_local
/cover
/deps
/exdoc
erl_crash.dump
*.ez

# Exact dependencies of testgear are determined per-antikythera-instance so not globally shareable
mix.lock

# Persist ANTIKYTHERA_INSTANCE_DEP in local filesystem
/.env
